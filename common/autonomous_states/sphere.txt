autonomy_state = {
	id = autonomy_sphere
	
	min_freedom_level = 0.95
	
	rule = {
        can_be_called_to_war = no
		can_not_declare_war = no
        can_create_factions = no
        can_join_factions = no
        can_join_factions_not_allowed_diplomacy = no
	}
	
	modifier = {
		extra_trade_to_overlord_factor = 0.1
		overlord_trade_cost_factor = -0.1
	}
	
	ai_subject_wants_higher = {
		factor = 0.0
	}
	
	ai_overlord_wants_lower = {
		factor = 1.0
	}

	ai_overlord_wants_garrison = {
		always = no
	}

	allowed = {
		OR = {
            OVERLORD = {NOT = {check_variable = { world_influence < PREV.world_influence }}}
            has_country_flag = SPHERE_STATE
        }
	}
	
	can_take_level = {
	}

	can_lose_level = {
        NOT = {has_country_flag = SPHERE_STATE}
	}
}